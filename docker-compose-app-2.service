# /etc/systemd/system/docker-compose-app-2.service
# thanks to oleg belostotsky on stack overflow for this 

[Unit]
Description=Docker Compose Application Service
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
# match the below to wherever you copied your docker-compose-2.yml
WorkingDirectory=/srv/docker
ExecStart=/usr/local/bin/docker-compose -f docker-compose-2.yml up -d
ExecStop=/usr/local/bin/docker-compose down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
